services:
  - type: web
    name: gmg-university
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npx serve dist -s -l $PORT
    envVars:
      - key: NODE_VERSION
        value: 18
    autoDeploy: true
